<?php
class GoogleAuth {
    static function verifyCredentials($accessToken, $expiresIn, $idToken) {
        // TODO: verify token with Google
        
        if (false) {
            throw new Exception("Provided Google credentials are invalid.");
        }

        return true;
    }
}